/**
 * Renderer-neutral text services shared by UI and other engine subsystems.
 *
 * <p>This package owns font loading, shaping, layout, Unicode boundaries, glyph atlas
 * preparation and Markdown parsing. GPU upload and visual effects remain the
 * responsibility of the consuming renderer.</p>
 */
package com.kaleblangley.haikalat.subsystems.text;

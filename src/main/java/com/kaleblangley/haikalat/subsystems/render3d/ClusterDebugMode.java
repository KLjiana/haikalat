package com.kaleblangley.haikalat.subsystems.render3d;

/** Debug visualisation modes rendered through the unified PBR material. */
public enum ClusterDebugMode {
    OFF,
    CLUSTER_ID,
    SLICE,
    LIGHT_COUNT,
    OVERFLOW,
    SHADOW_SLOT
}

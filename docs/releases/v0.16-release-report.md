# v0.16 发布验收报告

状态：`0.16.0`，已验收
日期：2026-07-19
环境：Windows 11、Java 21、LWJGL 3.3.3、OpenGL 4.6

## 已完成

- local/world bounds、stable camera frustum、独立 directional-shadow frustum、单次 updater
  SceneFrame 和 primitive stable Render Queue 已接入正式 `RenderPipeline`。
- diagnostics Overview/schema v1 已发布 visibility/queue 计数与分阶段时间，失败帧不复用旧统计。
- `SceneScalabilityDemo` 提供 100/1k/10k、enabled/disabled、small/medium/large/all-visible/
  all-hidden、有限帧、resize、diagnostics export 和五轮 benchmark。
- 10k large 达到 90% draw reduction；100 all-visible CPU overhead 在 1080p/4K 均低于 5%；
  all-hidden 热路径分配低于 64 KiB/frame。

## 已通过入口

| 命令 | 结果 |
| --- | --- |
| `test publicApiGuard architectureGuard --rerun-tasks` | 通过 |
| `localSceneVisibilityVerification --rerun-tasks` | 通过；16 tasks |
| `runSceneScalabilityBenchmarks --rerun-tasks` | 通过；1080p/4K、五轮、100 warmup + 1000 samples |
| `clean compileJava demoClasses test localGlVerification --rerun-tasks` | 通过；48 tasks |
| `localReleaseVerification --rerun-tasks` | 通过；35 tasks |
| `git diff --check` | 通过 |
| `runDemoIntegration` | 通过（开发中间检查） |

## 后续流水线复验

- 远端 Windows/Linux CI 仍由正式发布流水线复验，不在本地报告中伪造结果。
- 干净 release commit 与 tag 由仓库发布流程生成。

发布人已接受本地完整验证、人工视觉检查与成对性能证据，批准工程版本进入 `0.16.0`。

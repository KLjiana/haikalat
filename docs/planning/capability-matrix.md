# 核心能力矩阵

本矩阵记录当前实现事实。`完整` 表示已具备运行行为和自动化验证入口；`部分完成` 表示主路径可用但仍缺少端到端证明；`骨架` 表示接口或 pass 已存在，但最终渲染行为尚未闭环；`计划中` 表示已经进入明确版本范围，但尚未把计划当作实现事实。

| 能力 | 状态 | 源码入口 | Demo 入口 | 自动化验证 | 当前限制 |
| --- | --- | --- | --- | --- | --- |
| OpenGL 4.6 能力契约 | 完整 | `backend/GlCapabilityContract`、`core/device/GlRenderDevice`、`runtime/GlRenderThread` | 所有正式渲染入口共享设备/渲染线程边界 | `GlCapabilityContractTest`、`GlContextSmokeTest.hiddenWindowSatisfiesProductionCapabilityContract` | OpenGL 4.6 Core、DSA、SSBO、Compute、Image Load/Store、Buffer Storage、MDI、Shader Draw Parameters、debug output 为硬要求；bindless texture 仅报告为可选能力，不提供旧版降级 |
| 外部宿主可嵌入渲染 | 引擎侧完整 | `PresentationTarget`、`ExternalAttachment`、`ExternalCamera`、`HaikalatRuntime`、`RenderGraph`、`RenderPipeline`、`UiRenderer` | Haikalat 主工程不新增窗口 Demo；`EmbeddedPresentationGlTest` 以隐藏 GL context 模拟宿主 raw FBO，HaikalatHost 负责 Minecraft 烟雾测试 | `embeddedJvmVerification`、`embeddedGlVerification`、`ArchitectureBoundaryTest`、原有 `RenderPipelineGlTest` / `UiRendererGlTest` | 非零 FBO、宿主 color/depth、target replacement、zero extent、borrowed 生命周期、UI/VFX/postprocess 同目标、外部相机和成功/异常 GL 状态恢复已验证；Minecraft/Blaze3D Java 状态同步与真实客户端烟雾仍由 HaikalatHost 完成 |
| RenderGraph 排序与资源生命周期 | 完整 | `core/graph/RenderGraph`、`backend/framebuffer/RenderTargetManager` | 三个 Demo | `RenderGraphTest`、`FramebufferDescriptorTest`、`RenderPipelineGlTest`、`GlContextSmokeTest` | framebuffer/target 重建采用 candidate-first；窗口 resize 只替换 window/relative target，fixed-size shadow target 保持；失败时旧 generation 可用；GPU timing 使用正式 query opcode |
| 通用资源身份、代次与异步 CPU 解码 | 完整 | `subsystems/resources` | 由后续 glTF skin/animation 接入消费 | `AssetIdTest`、`ResourceSourceTest`、`ResourceGenerationTrackerTest`、`AsyncResourceDecoderTest`、架构边界测试 | 当前只负责编码字节与 CPU 值；GPU 上传必须携带代次票据并在渲染线程再次校验，不在该 subsystem 内调用 GL |
| 序列化场景 v1、角色 descriptor、异步 glTF/图片加载与热重载 | 完整 | `subsystems/scene`、`SceneCharacterDefinition`、`CharacterBuildPlan`、`SceneAssetService`、`GltfGpuAssetCache`、`SceneVersion`、`AnimationGraphParser/Compiler`、`SceneReloadCoordinator` | `SerializedSceneDemo`、`runSerializedSceneIntegration`、`runSerializedSceneStabilityIntegration`；embedded host 复用同一 CPU plan | `sceneJvmVerification`、`sceneCharacterJvmVerification`、`localSceneAssetVerification`、`SerializedSceneGlTest`、`SceneJsonParserTest`、`SceneAssetServiceTest`、`AnimationGraphParserTest`、`SerializedSceneStabilityTest`、`EmbeddedPresentationGlTest` | 已闭环严格 schema、characters model/library/Graph dependency、`haikalat.animation-graph/1` JSON sidecar、`.gltf/.glb/animation-library.json` 失效、RGBA8 后台解码、分阶段上传、exact-generation lease、Graph candidate 解析、失败保留旧版本、同拓扑 RenderGraph fast path、UI diagnostics、3,600 帧 + 100 次 reload soak、close 竞争和重复 close；v0.21.0-beta 的 10k 对象 CPU 回退已由 v0.21.1 恢复，具体基准见 `docs/releases/v0.21.1-release-report.md` |
| 材质与纹理 | 完整 | `TextureColorSpace`、`Texture2D`、`core/material`、`TextureAssetCache` | `LearnOpenGlDemo`、`MinimalDemo` | `MaterialTest`、`AssetPipelineTest`、`RuntimeResourceGlTest` | 旧纹理 API 默认 linear；颜色纹理需在 manifest/API 显式声明 sRGB，数据纹理保持 linear |
| Metallic-roughness PBR | 完整 | `MaterialModel`、`PbrMaterialProperties`、`TangentGenerator`、`PbrMaterials`、PBR forward shader | `PbrDemo`、`Render3dV023Demo`、`LearnOpenGlDemo` PBR proof | `AssetPipelineTest`、`TangentGeneratorTest`、`PbrBrdfMathTest`、`PbrEnvironmentGlTest`、`GltfRuntimeGlTest`、`runPbrCompatibilityIntegration`、`runRender3dV023Integration` | glTF scene-asset 的 OPAQUE/MASK/BLEND 已映射到固定 PBR shader/unit contract；MASK 在主 pass 与阴影 pass 使用相同 alpha cutoff，BLEND 进入稳定透明队列且默认不投射阴影；不承诺 transmission 或高级材质扩展 |
| GTAO 与时域环境遮蔽 | 实现完成（v0.23.3） | `GtaoSettings`、`GtaoQuality`、`GtaoPasses`、`GtaoHistory`、`RenderPipeline`、R8/RenderGraph preview、PBR indirect shader | `Render3dV023Demo --gtao`、独立 `Render3dGtaoDemo`、`runRender3dGtaoIntegration`、`runRender3dGtaoBenchmarks` | `GtaoSettingsTest`、`GtaoPassesTest`、`PipelineTopologyTest`、`FramebufferDescriptorTest`、`RenderGraphTest`、`RenderGraphResizeCandidateTest`、`GtaoShaderGlTest`、`GtaoHistoryResizeGlTest`、`RenderPipelineGlTest`、`GltfRuntimeGlTest`、完整 `glSmoke` | 全分辨率深度预通过按材质 cull/mirrored silhouette，半分辨率 horizon estimate、temporal 线性深度与 deterministic frame phase、法线权重 denoise/upsample、独立 RG16F history、unit 13 和 PBR 间接光均已接入；静止 view 保持稳定 phase，只有真实 view matrix 移动时才轮换采样，且忽略 TAA jitter；RenderGraph/TAA/GTAO history 采用 generation-wide candidate prepare/commit，失败注入不会提前替换活动尺寸且下一次 resize 可恢复；GTAO GL 用例覆盖像素 A/B、NONE/MSAA/FXAA/TAA、相机 disocclusion、实例阴影、skin/morph 与失败恢复；benchmark 直接门禁稳定 CPU/GPU/allocation，并记录 `v0.23.1@4cc6d44` 历史对照；4K disabled GPU 的历史回退作为分辨率/配置成本记录并按发布策略接受；默认关闭、仅 metallic-roughness PBR 正式保证 |
| 风格化室外环境与体积阳光 | 已完成（v0.24.0 候选） | `OutdoorEnvironmentSettings`、`StylizedSkySettings`、`VolumetricSunSettings`、`LocalFogVolume`、`OutdoorVolumetricSunPass`、`PostProcessPassBuilder`、`RenderPipeline`、`OutdoorEnvironmentOverlay` | `runOutdoorEnvironmentDemo`、`runOutdoorEnvironmentIntegration`、`runOutdoorEnvironmentReferenceIntegration`、`runOutdoorEnvironmentStabilityIntegration`、`runOutdoorEnvironmentBenchmarks`、`localOutdoorVerification` | `OutdoorEnvironmentSettingsTest`、`OutdoorVolumeMathTest`、`compileJava demoClasses test`、专项 integration/stability/reference/benchmark | HDR + OPAQUE/MASK + 主太阳 4 级 CSM、三种天空/环境预设、有限距离体积积分、独立 RGBA16F/R16F 重投影历史、深度感知上采样和最多 8 个球/盒局部雾已接入；可见面板可调参数、查看深度/CSM/散射/透射率/历史诊断并保存/重载复现配置；LDR、ALPHA/ADDITIVE、HDR VFX 组合明确拒绝；五轮 1080p/4K 配对、稳定分配、3,600 帧/100 切换/10 失败注入均有专项证据；透明/VFX 体积散射仍不在 v0.24.0 |
| glTF 2.0 资产、外部动画库、GPU 蒙皮与 Morph Target | 完整 | `core/assets/gltf`、`subsystems/render3d/gltf`、`SceneAssetConfig.gltfScenes` | `GltfDemo` + two-joint/morph/player_wild fixture、主 Demo showcase | `GltfAssetLoaderTest`、`GltfAnimationLibraryTest`、`GltfRuntimeGlTest`、`runGltfIntegration`、`runGltfSkinningIntegration`、`runGltfMorphIntegration`、`runGltfMorphSkinningIntegration`、`runPlayerWildIntegration`、`localGltfVerification` | OPAQUE/MASK/BLEND `.gltf/.glb`、external/data/GLB embedded 资源、`haikalat.gltf-animation-library/1` ZIP/目录清单、animation-only glTF 与 `haikalat.animation-clip/1` 紧凑外部 clip 合并、共享 `GltfAnimationSet` 的独立 GLB 动画源、多模型严格 Rig 绑定、四影响 skin、POSITION/NORMAL/TANGENT Morph、mesh/node weights 与 STEP/LINEAR/CUBICSPLINE weights animation；Morph 先于 skin，deformation revision 会失效 bounds，PBR 与 directional shadow 均闭环；每 primitive 最多 8 targets，不支持 GeckoLib 原始 Molang `.animation.json`、第二组关节权重、compute morph 或 GPU animation sampling |
| 确定性 Animation Graph 与约束运行时 | 完整 | `subsystems/animation`、`GltfAnimationRig`、`Skin`、`JointPalette`、`AnimationGraphParser`/`AnimationGraphCompiler` | `AnimationDemo --scenario=clip|graph|additive|constraints|all`、`GltfDemo`、`SerializedSceneDemo`、`HaikalatShowcaseDemo` | `animationJvmVerification`、`animationGlVerification`、`animationPerformanceVerification`、`runShowcaseAnimationIntegration`、`sceneCharacterJvmVerification` | 保留 Player/Mixer 兼容入口；`GltfSceneInstance.transitionTo(...)` 可从当前可见姿势生成直接 clip 过渡并支持连续重定向；typed parameter/trigger/state/transition/interruption、1D/显式 triangle 2D Blend Tree、最多 8 层 Override/Additive、Marker/Sync/Signal、倒放和约束已闭环；v0.21 增加 `haikalat.animation-graph/1` JSON sidecar、candidate-first controller attach 和真实 `player_wild` 绑定；内部 signal 使用有界优先级队列并复用 cursor/run-list 容量；不承诺编辑器、motion matching、物理/场景查询或 full-body IK |
| 标量曲线、颜色渐变与空间路径 | 完整 | `core/curve` | `VfxDemo`、`HaikalatShowcaseDemo` | `CurveCoreTest`、`UiAnimationSystemTest`、`runVfxCurveBenchmark` | `Curve1f` 输入限定 `[0,1]`，输出允许 overshoot；支持 cubic preset、spring、正确反解 x 轴的 cubic-bezier、STEP/LINEAR/Hermite `FloatTrack`、线性 HDR RGBA gradient、固定弧长表 BezierPath3f 和 LUT；不包含可视化曲线编辑器 |
| HDR cubemap 与 GPU IBL | 完整 | `TextureCube`、typed cube commands、`EnvironmentPreprocessor`、`PbrEnvironment` | `PbrDemo` environment/background | `PbrEnvironmentGlTest`、`runPbrIntegration`、`runPbrResizeIntegration`、`runPbrFailureIntegration` | 单一全局 environment；启动期 compute 预计算；生产路径无 readback/glFinish，resize 不重建 environment |
| 方向光、点光、聚光参数 | 完整 | `SceneLight`、`LightingBinder`、Demo forward shader | `LearnOpenGlDemo` | `RenderPipelineTest`、`ScenePipelineTest`、`GlContextSmokeTest` | shader 数组上限为方向光 2、点光 8、聚光 4；shadow light index 与有界方向光数组使用同一选择结果 |
| 方向光阴影 | 完整 | `DirectionalShadowMap`、`DirectionalCascadeSettings`、`DirectionalCascadePlan`、`ShadowFramePlan`、`ShadowCasterPlanner`、`ShadowCacheState`、`RenderPipeline` | `LearnOpenGlDemo`、`Render3dV023Demo`、`Render3dShadowBudgetDemo`、`runRender3dDynamicShadowIntegration` | `DirectionalCascadeSettingsTest`、`ShadowCacheStateTest`、`ScenePipelineTest`、`SceneVisibilityGlTest`、`LocalShadowsGlTest`、`RenderPipelineGlTest`、`runRender3dV023Integration`、`runRender3dShadowBudgetIntegration`、`runRender3dDynamicShadowIntegration` | 默认保持单级方向光图；高级配置可启用 2～4 级 fixed atlas、practical split、XYZ world-texel snapping、HARD/3×3/5×5 tile-safe PCF 与 split blend；每级使用保守 AABB/frustum caster slice，静止/子 texel 相机和同宽高比 resize 可复用 cascade depth，相机有效移动只失效受影响方向 tile；未知 bounds 走 volatile 全量回退 |
| 点光/聚光阴影与级联方向光 | 完整 | `LocalShadowPipelineSettings`、`ShadowLightHints`、`ShadowLightScheduler`、`ShadowFramePlan`、`ShadowCasterPlanner`、`PointShadowAtlas`、`SpotShadowAtlas`、`ShadowSamplingBlock`、`ShadowCacheState`、`RenderPipeline` | `PbrDemo --local-shadows=on`、`Render3dShadowBudgetDemo`、`runRender3dDynamicShadowIntegration` | `ShadowLightSchedulerTest`、`ShadowCacheStateTest`、`LocalShadowMathTest`、`LocalShadowsGlTest`、`ScenePipelineTest`、`RenderPipelineGlTest`、`runLocalShadowsIntegration`、`runRender3dShadowBudgetIntegration`、`runRender3dDynamicShadowIntegration` | legacy 默认保持 1 point + 1 spot 与 v0.23.0 uniform/资源合同；显式 balanced 支持 1 directional + 2 point + 4 spot、stable ID/槽位、priority/相机评分/迟滞/拒绝原因、逐 point-face dirty/cache、逐 view caster slice、dirty empty clear、1,392-byte sampling UBO、HARD/3×3/5×5 PCF 和统一 bias。point 为两个独立 3×2 block，spot 为 2×2 atlas；不支持第二盏方向光 CSM、无限阴影灯、软/透明阴影或 clustered lighting |
| Instancing | 完整 | `InstancedMeshBatch`、`InstancedRenderer`、`CommandBuffer.drawInstancedBatch/prepareInstancedBatchPersistent` | 三个 Demo | `CommandBufferTest`、`InstanceDataLayoutTest`、`InstanceUploadGlTest.preparedMultiPassBatchIsFencedWhenGeometryStageFailsAndNextFrameReusesRing`、`RenderPipelineGlTest` GTAO+实例阴影组合 | 实例阴影显式 opt-in；shadow/GTAO depth/geometry 复用一次快照与 ring 上传，最后一次 draw 后统一插入 fence；命令失败时持久批次 abort 并释放所有未完成快照 |
| None / MSAA / FXAA / TAA | 完整 | `PostProcessPassBuilder`、`DepthResolveDescriptor`、`subsystems/postprocess` | `LearnOpenGlDemo`、`MinimalDemo`、`Render3dV023Demo` | `DepthResolveDescriptorTest`、`RenderPipelineTest`、`ScenePipelineTest`、`RenderPipelineGlTest`、`runMinimalIntegration`、`runRender3dV023Integration` | LDR 使用 `SRGB8_ALPHA8` scene/final/history target 并复制编码结果到 backbuffer；HDR 中 MSAA 分别显式 resolve color/depth，TAA 在线性空间累积，FXAA 在 tone mapping 后执行；借用宿主 depth 时会校验 sample compatibility |
| HDR / ACES tone mapping | 完整 | `RenderSettings`、`ToneMappingMode`、`ToneMappingPass`、`RGBA16F` framebuffer | `LearnOpenGlDemo` | `RenderSettingsTest`、`ToneMappingPassTest`、`FramebufferDescriptorTest`、`RenderPipelineGlTest` | 默认保持 LDR；ACES 显式 gamma 并关闭 framebuffer sRGB |
| 全 GPU 自动曝光 | 完整 | `ExposureMode`、`AutoExposureSettings`、`AutoExposurePass`、`R16F/RG32F` framebuffer | `runAutoExposureDemo`、`runAutoExposureIntegration` | `AutoExposurePassTest`、`AutoExposureGlTest`、`RenderPipelineGlTest` | 默认 MANUAL；AUTO 只用于 HDR；固定 14 级 sum/weight reduction，最大正式边长 16384；全程留在 GPU，不向标题回读曝光数值 |
| Bloom | 完整 | `BloomSettings`、`BloomPass`、RenderGraph relative target | `runBloomDemo` | `RenderGraphTest`、`RenderPipelineTest`、`RenderPipelineGlTest`、`runBloomIntegration` | 默认关闭；1～8 层 HDR downsample/upsample，最终半分辨率纹理直接由 ToneMapping 合成 |
| Color Grading LUT 与距离/高度雾 | 完整 | `subsystems/postprocess/ColorGradingLut`、`FogPass`、`PostProcessSettings`、`DepthResolveDescriptor` | `PbrDemo --color-grading=on --fog=on`、`Render3dV023Demo` | `ColorGradingLutTest`、`FogPassTest`、`DepthResolveDescriptorTest`、`PostProcessEffectsGlTest`、`RenderPipelineGlTest`、`runPostProcessEffectsIntegration`、`runRender3dV023Integration` | LUT 在 ACES 与 gamma 之间采样；雾由单采样场景深度重建世界坐标并在 tone mapping 前合成；4× MSAA 通过显式 depth resolve 后供 Fog 读取，resize 会事务式替换相关 target |
| Retained-mode 游戏 UI | 完整 | `subsystems/ui/UiSystem`、`UiDocument`、Yoga layout、widget/event/style class、`UiRenderer`、`UiSdfShape`、`UiCompositor` | `UiDemo`、独立双语游戏主界面 `ModernUiDemo`、`LearnOpenGlDemo` overlay | `UiSystemTest`、`ModernGameMenuSceneTest`、widget/layout/event tests、`UiRendererGlTest`、`UiSdfPrimitiveTest`、`UiCompositorFoundationTest`、`runModernUiIntegration`、五项 UI integration | 节点可携带 style class 并由 `UiConfig` 注入 resolver；普通 quad/image/glyph 路径保持兼容；静态树复用已发布 snapshot，稳定 hit-test 和未变化 virtual-list viewport 走缓存/no-op fast path；analytic SDF 与显式 layer topology 保持可用，offscreen subtree replay/cache reuse 仍为 advanced 后续项 |
| UI Tween / Transition | 完整 | `subsystems/ui/animation`、`UiSystem.animations()`、`UiSystem.timeline()` | `UiDemo`、`ModernUiDemo`、`HaikalatShowcaseDemo` | `UiAnimationSystemTest`、`UiModernAnimationTest`、`UiDemoGlTest`、`runUiIntegration`、`runModernUiIntegration` | 兼容 visual/layout transition；新增 property tracks、仿射 transform/逆 hit-test、sequence/group/stagger/repeat/reverse、signal、interaction state、enter/exit、reduced motion 与显式 FLIP；rotated GPU clip 使用保守 scissor AABB |
| Screen-space UI VFX | 完整 | `subsystems/ui/vfx`、`UiSystem.effects()` | `ModernUiDemo` | `UiEffectRuntimeTest`、3,600 帧 JVM soak、`runModernUiIntegration` | fixed-seed shimmer/ripple/dissolve/spark/confetti/trail/scanline/glitch；signal 去重、容量、detach、clip 和 reduced-motion 明确。复用 UI SDF/quad ring，尚无独立 instance buffer、sprite atlas 或 compute |
| CPU 纹理化 HDR VFX | 完整 | `subsystems/vfx`、`subsystems/render3d/vfx/VfxRenderer`、`RenderPipeline.hdrVfx` | `VfxDemo`、`HaikalatShowcaseDemo` | `EffectInstanceTest`、`VfxMaterialTest`、`VfxTextureCacheGlTest`、`VfxRendererGlTest`、`runVfxLegacyIntegration`、`runVfxFireIntegration`、`runVfxImpactIntegration`、`runVfxTrailIntegration`、`runShowcaseVfxMsaaIntegration`、`runShowcaseVfxAutoExposureIntegration` | Particle/Ribbon/平面 Decal/MeshVfx 支持显式 mask、R8/sRGB 纹理、Alpha/Additive、emissive、billboard/stretch、soft particle 和 HDR Bloom；Particle 支持规则 atlas flipbook，Ribbon 复用 TRAIL/BEAM 两种控制点来源；保持稳定透明顺序且仍逐 primitive draw。尚未实现 Ribbon repeat/scroll UV、VFX fog 计算、Distortion、动态光或目标材质效果 |
| GPU 粒子与体积光受控实验 | 完整 | `GpuParticleExperiment`、`VolumetricLightPass`、`VolumetricLightSettings` | `HaikalatShowcaseDemo` | `VolumetricLightPassTest`、`GpuEffectsGlTest`、`runShowcaseIntegration`、`runShowcasePerformanceBaseline`、`runShowcaseStabilityIntegration` | Compute + SSBO 粒子无 CPU readback，容量上限 1,048,576；体积光固定 4～128 步。两者仍为 advanced 实验，不含粒子排序/碰撞/indirect compact，也不含深度/阴影/时域体积积分 |
| Latin/CJK 文本、受控 CommonMark 显示子集与 glyph atlas | 完整 | `subsystems/text`、`TextSystem`、`MarkdownParser`、`MarkdownDocument`、`GlyphAtlas`、`UiTextEngine`、`UiGlyphAtlasGpu` | `MarkdownTextDemo`、`runMarkdownTextIntegration`、`runTextBenchmarks` | `MarkdownParserTest`、`TextSystemTest`、native shaping/font/atlas tests、`UiGlyphAtlasGpuGlTest`、架构门禁 | CommonMark parser 驱动的受控显示子集：H1-H3、paragraph/quote、扁平 list 文本、code、strong/emphasis/inline code 与换行；H4-H6 映射 H3，link 只保留文本，image/URL/嵌套层级/序号/语言/HTML/table/thematic break 不承诺；输入上限 1,048,576 字符；完整 BiDi、彩色 emoji、variable-axis UI 不承诺 |
| UI 输入、焦点、clipboard 与 Windows IME | 完整 | `WindowInputSnapshot`、`UiInputRouter`、`TextField`、`TextInputAdapter`、`Win32TextInputAdapter` | `UiDemo`、`runUiInteractiveSoak` | input/focus/TextField tests、可注入 IME integration、`runUiSyntheticImeSoak`、`runUiNativeSoak`、2026-07-17 Microsoft Pinyin 人工矩阵 | committed char 与 composition 分离；非 Windows 平台降级为 committed-char；Emoji 输入可提交，但内建字体不保证对应字形且不支持彩色 Emoji |
| OBJ 模型与场景配置 | 完整 | `ObjModelLoader`、`ModelAssetManager`、`SceneAssetConfig` | `LearnOpenGlDemo` | `AssetPipelineTest`、`DemoResourceContractTest`、`runDemoIntegration` | 主 Demo 从 classpath manifest 加载自有 OBJ，并将多 mesh 映射为共享材质/transform 的 renderer |
| 异步上传与渲染线程 | 完整 | `UploadSystem`、`LatestFrameMailbox`、`GlRenderThread` | `AsyncDemo` | `UploadSystemTest`、`LatestFrameMailboxTest`、`GlContextSmokeTest`、`runAsyncIntegration` | 两线程 latest-wins；矩阵 UBO 上传成功后才发布对应不可变帧状态 |
| CPU/GPU profiling | 完整 | `FrameDriver`、`GpuTimer`、`FrameProfile` | `LearnOpenGlDemo` 标题 | `ObservabilityTest`、`RenderGraphTest`、`runDemoIntegration` | RenderGraph profiling 不再依赖 `custom()` escape hatch |
| 调试与可观察性闭环 | 完整 | `runtime/diagnostics`、`Render3dDiagnostics`、`RenderGraph.description`、`GlDebug` message/resource snapshot | `LearnOpenGlDemo` F2 五页面板、`Render3dV023Demo` 标题栏/摘要、`Render3dShadowBudgetDemo` shadow budget/cache 标题 | `FrameDiagnosticsTest`、`RenderGraphDescriptionTest`、`RenderPipelineTest`、`EmbeddedPresentationGlTest`、`runRender3dV023Integration`、`runRender3dShadowBudgetIntegration`、`localDiagnosticsVerification` | 通用诊断保持有界 240-frame/256-message；Render3D snapshot 覆盖 revision/invalidation、generation、queue/visibility/depth-resolve/cache/failure-stage，并为阴影报告 candidate/selected/capacity、stable ID、shader index、slot、score/reject、tile redraw/reuse、cache miss reason、filter、atlas 和估算 depth bytes；不保存逐对象列表，不编辑 graph、不控制外部 debugger |
| RenderGraph GPU 资源预览 | 完整 | `render3d/preview`、typed framebuffer blit、`PreviewSummary` | F2 Graph 资源行、preview pane 与显示控件 | `PreviewContractsTest`、`GraphPreviewGlTest`、`runPreviewIntegration` | 支持 LDR/sRGB/HDR/R/RG/depth、MSAA resolve 和显式 PBR cubemap；backbuffer/任意外部资源不支持；冻结只冻结元数据，不捕获像素 |
| 普通场景可见性与 Render Queue | 完整 | `Bounds3f`、`SceneFrameBuilder`、`Frustum`、`RenderQueueClass`、`RenderQueueSorter` | `SceneScalabilityDemo`、`GltfSceneScalabilityDemo`、`Render3dV023Demo` | `RenderQueueClassTest`、`RenderQueueSorterTest`、`TransformRevisionTest`、`SceneVisibilityGlTest`、`runRender3dV023Integration`、`localSceneSubmissionVerification` | OPAQUE/MASKED 使用稳定状态排序，ALPHA 使用 camera-space back-to-front 稳定排序，ADDITIVE 保持提交顺序；revisioned/fixed renderer 复用 model/bounds/queue，deformation revision 会失效 bounds。10k 基准中 O(N) culling + queue scan 未持续超过 2 ms，因此 v0.23 不实施 CPU spatial index；仍不包含层级 bounds、occlusion、GPU culling 或 MDI |

## 构建基线

- Java：Gradle Toolchain 固定为 21。
- 默认命令：`compileJava demoClasses test`。
- 正式稳定版本：`0.24.0`；上一正式稳定版本为 `0.23.4`。
- v0.24 室外能力已完成候选验证，构建版本为 `0.24.0`；正式 tag 应指向最后一次 `releaseReadiness` 通过的提交。
- 默认测试：纯 JVM 测试；真实 GL 类通过 `haikalat.glSmoke=true` 显式启用。
- CI：Windows 与 Linux 均执行无窗口编译和纯 JVM 测试。
- 本地真实 GL：`test -Dhaikalat.glSmoke=true --rerun-tasks`，要求桌面环境与 OpenGL 4.6 驱动。
- UI 本地验收：`localUiVerification`，执行 GL smoke、SDF、deterministic/resize/ModernUi/async/text integration、100 轮 synthetic IME soak 与至少 50 轮 Windows native hook soak。
- PBR 本地验收：`localPbrVerification`，执行 PBR JVM/真实 GL、deterministic、resize、legacy compatibility 与 failure cleanup。
- glTF 本地验收：`localGltfVerification`，执行 parser/accessor/node/material/skin/animation/Morph JVM 测试、真实 GL upload/lifecycle/PBR-shadow skinning + Morph，以及 deterministic/skinning/resize 集成。
- 后处理效果验收：`runPostProcessEffectsIntegration` 与 `PostProcessEffectsGlTest` 验证 LUT、距离/高度雾、像素变化和 resize。
- UI 动画/VFX 验收：默认 `test` 验证 easing、property/transform、timeline signal、compositor 数据、固定种子、容量与 3,600 帧 soak；`runUiIntegration` 和 `runModernUiIntegration` 验证实际 snapshot、managed layer topology 与最终像素。
- VFX 本地验收：默认 `test` 覆盖固定种子、容量、寿命、over-life、材质验证、排序和资产关闭；真实 GL 覆盖 R8/sRGB cache、legacy LDR、fire/smoke Alpha+Additive、impact、trail、HDR/Bloom、soft particle、MSAA depth resolve、自动曝光顺序和 UI 隔离；1080p/4K 入口输出 CPU/GPU/draw/uniform 基线。
- 多类阴影验收：`LocalShadowMathTest` 验证六面/外锥/级联矩阵，`LocalShadowsGlTest` 做有阴影/无阴影 PBR 像素 A/B，`runLocalShadowsIntegration` 串联方向光、点光与聚光 pass。
- Milestone 4 验收：`localMilestone4Verification` 汇总高级动画 JVM 测试、GPU effects 真实 GL、24 帧综合像素场景和 3600 帧资源稳定性；`runShowcasePerformanceBaseline` 记录 960×540 / 2048 GPU 粒子基线。
- 动画验收：默认 `check` 包含 `animationJvmVerification`；`localAnimationVerification` 统一执行 Graph/layer/constraint/VFX bridge、真实 GL Morph 与五轮 1/100/1000 角色、4/8 target 基准；`runAnimationIntegration` 保留 Player 兼容路径。
- 资源基础验收：默认 `test` 覆盖逻辑路径、source 上限、代次失效和并发解码过期发布；`GlContextSmokeTest` 同时验证生产 OpenGL 4.6 能力契约。
- diagnostics 本地验收：`localDiagnosticsVerification`，执行 history/export JVM 测试以及 deterministic、resize、failure/recovery 三项真实 GL capture。
- preview 本地验收：`previewGlVerification` 与 `runPreviewIntegration`；二者已接入 `localGlVerification`。
- scene visibility 本地验收：`localSceneVisibilityVerification`，执行 bounds/frustum/queue JVM 测试、camera/shadow/resize 真实 GL 回归和 10,000 renderer 有限帧门禁。
- scene submission 本地验收：`localSceneSubmissionVerification`，执行 revision/mat4 arena JVM 测试、
  queue/TAA/像素真实 GL、共享 glTF 100/1k、resize/motion、10k allocation 和静态 all-hidden 门禁。
- Render3D v0.23 验收：`runRender3dV023Demo` 提供可交互窗口，
  `runRender3dV023Integration` 在隐藏窗口中验证 OPAQUE/MASKED/ALPHA/ADDITIVE、MASK shadow、
  Fog + 4x MSAA depth resolve、4 级 CSM、resize 和有界 diagnostics；两者的阴影 atlas 均为
  4096x4096，每个 4 级 cascade tile 为 2048x2048。
- serialized scene 本地验收：`localSceneAssetVerification`，执行严格 JSON/catalog/依赖/图片 CPU
  解码 JVM 测试，以及 staged upload、generation lease 和事务式场景替换真实 GL 回归。
- 完整本地验收：`localGlVerification`，包含 `localUiVerification`，并继续运行基准/resize/Bloom/自动曝光/Minimal/Async/空窗口 Demo 和压力入口。

矩阵状态随实现阶段更新；未完成端到端验证的能力不得在 README 中描述为完整效果。
